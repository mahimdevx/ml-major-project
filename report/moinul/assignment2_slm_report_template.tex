\documentclass[10pt,conference]{IEEEtran}

% ============================================================
% Packages
% ============================================================
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{times}
\usepackage{microtype}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{array}
\usepackage{tabularx}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{hyperref}
\usepackage{minted}
\usepackage{amsmath}
\usepackage{amssymb}

\hypersetup{
    colorlinks=true,
    linkcolor=blue,
    citecolor=blue,
    urlcolor=blue
}

\setlist[itemize]{leftmargin=*, itemsep=2pt, topsep=2pt}
\setlist[enumerate]{leftmargin=*, itemsep=2pt, topsep=2pt}
\setminted{fontsize=\footnotesize,breaklines=true,frame=single,autogobble=true}

% ============================================================

% ============================================================
% Title
% ============================================================
\title{
Assignment 2 Report Template:\\
Domain Adaptation with Small Language Models
}

\author{
\IEEEauthorblockN{
Group Number: \emph{Insert Group Number}
}
\IEEEauthorblockA{
CSCI433/933 Machine Learning Algorithms and Applications\\
Student Names and Student Numbers: \emph{Insert Details}\\
}
}

\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

\begin{document}
\maketitle

% ============================================================
\section{Methodology}
\label{sec:methodology}

This project adapts Gemma~3~4B, a pretrained small language model 
running locally via Ollama, for Shakespeare question answering using 
retrieval-augmented generation (RAG) as the primary adaptation strategy. 
Rather than modifying the model's weights, the system conditions the 
model's responses on passages retrieved directly from the Shakespeare 
corpus at query time. Three systems were implemented and compared: a 
prompt-only baseline that relies solely on the model's pretrained 
knowledge, a standard RAG system that retrieves relevant utterances 
from an indexed corpus before generating an answer, and an enhanced 
RAG system that extends retrieval with cross-encoder reranking and 
scene-level diversity filtering. All three systems use the same language 
model and hardware setup, isolating retrieval and reranking as the 
variables under evaluation.

\subsection{Baseline System}

The baseline system is implemented in \texttt{baseline.py} and performs no retrieval. A user
question is formatted using a prompt template and sent directly to Gemma~3~4B via Ollama.
The model generates its answer solely from its pretrained weights, with access only to the
user's question and its general training knowledge --- it has no access to the Shakespeare
corpus, retrieved passages, or any scene metadata at query time.

This is a prompt-only baseline rather than a scene-level retrieval baseline. This choice was
deliberate: a no-retrieval baseline produces a cleaner separation between the two systems,
making the contribution of retrieval directly measurable. When the model lacks retrieved
context, failures such as hallucination, vague answers, and unsupported claims become visible
and attributable to the absence of retrieval rather than to poor retrieval quality.

The baseline uses the same language model, hardware, and prompt structure as the improved
system. This ensures that any difference in output quality between the two systems is
attributable to the presence or absence of retrieval, not to differences in model capability
or infrastructure.

\subsection{RAG-Based System}

\subsubsection{Standard RAG-Based System}

The improved system retrieves from the \texttt{shakespeare\_utterances} ChromaDB collection,
built by the data engineering pipeline. Each utterance was indexed using an enriched
embedding string combining the speaker, utterance text, scene summary, and keywords ---
providing the embedding model sufficient semantic context to represent short archaic lines
meaningfully in vector space. The RAG pipeline queries this pre-built index directly at
runtime, with no recomputation required.

At query time, the user's question is embedded using
\texttt{sentence-transformers/all-MiniLM-L6-v2}, which maps any text string into a
384-dimensional vector space. This vector is compared against the indexed utterance vectors
via ChromaDB's approximate nearest-neighbour index. Because the embedding model was trained on large modern English
corpora, it understands semantic proximity between contemporary
query language and archaic Shakespearean phrasing --- meaning a
query such as ``Why does Macbeth kill Duncan?'' can surface relevant
passages even when the exact words ``kill'' or ``Duncan'' do not
appear in them. This works because words such as ``kill'',
``murder'', and ``do the deed'' occupy nearby regions of vector
space regardless of surface form. The enriched embedding strings
--- which include modern English scene summaries and keywords
alongside the archaic utterance text --- further bridge the
vocabulary gap, ensuring that short Shakespearean lines are placed
in semantically meaningful positions rather than isolated by their
archaic phrasing.
The top-$k$ results are returned with full
metadata including play, act, scene, speaker, and scene summary. Retrieved passages are
always displayed to the user before the generated answer, regardless of interaction mode.
This guarantees transparency --- the user can always see what context the model was given
before reading its response.

The system supports four interaction modes. In \texttt{qa} and \texttt{concept} modes,
retrieved utterances are formatted into a structured context block --- including provenance
labels and scene context --- and injected into a prompt template alongside the user's
question. In \texttt{evidence} mode, generation is skipped entirely and only the retrieved
passages are displayed. In \texttt{stylised} mode, a separate prompt template requests a
Shakespearean-style response limited to 150 words, and the output is explicitly labelled as
creative rather than factual, clearly distinguishing it from evidence-based answers. Prompt
templates are stored as external files, decoupling prompt design from pipeline logic and
allowing iterative refinement without code changes.

\subsubsection{Enhanced RAG-Based System}

An enhanced pipeline extends the standard system with a two-stage retrieve-then-rerank
approach. In the first stage, ChromaDB retrieves 15 broad candidates using the bi-encoder,
where query and document vectors are computed independently and compared by cosine
similarity. While fast, this approach can miss fine-grained relevance because the query and
document are never directly compared against each other. In the second stage, a cross-encoder
model (\texttt{cross-encoder/ms-marco-MiniLM-L-6-v2}) addresses this by jointly processing
each query--document pair as a single input, allowing the model to attend to the interaction
between query terms and document terms directly. This produces more accurate relevance scores
at the cost of higher computation, which is why the cross-encoder is applied only to the
shortlisted 15 candidates rather than the full collection. Candidates are then re-sorted by
cross-encoder score before the top-5 are selected for generation.

A scene-level diversity constraint of at most two results per scene is applied after
reranking. This prevents a single highly relevant scene from dominating the context block,
which is particularly important for multi-act questions such as Q3 (Hamlet's delay), where
evidence spans several separate scenes across the play. That said, this constraint is a
known limitation: for focused single-scene questions, it may discard highly relevant
passages in favour of less relevant ones from other scenes. A minimum coverage constraint
--- guaranteeing at least one result per act rather than capping per scene --- may be a
more appropriate strategy and is identified as a direction for improvement.

\subsection{Model Choice and Justification}
 
Gemma~3~4B was selected as the language model for both the baseline and improved systems,
running locally via Ollama. Table~\ref{tab:model_comparison} summarises the five candidate
models evaluated before this decision was made.
 
\begin{table}[ht]
\centering
\caption{Candidate model comparison. RAM figures sourced from official
Ollama model pages~\cite{ollama_gemma3,localaimaster_ram}.
RAG evaluation score reflects whether the model's limited domain
knowledge makes retrieval genuinely necessary.}
\label{tab:model_comparison}
\begin{tabular}{llcc}
\hline
\textbf{Model} & \textbf{Type} & \textbf{RAM} & \textbf{RAG eval genuine} \\
\hline
\textbf{Gemma 3 4B}  & Local & $\sim$2.5\,GB~\checkmark & 5/5 \\
Mistral 7B Q4        & Local & $\sim$4.3\,GB~$\approx$  & 5/5 \\
Gemma 4 E4B          & Local & $\sim$8\,GB~$\times$     & 5/5 \\
Phi-4 Mini           & Local & $\sim$2.3\,GB~\checkmark & 5/5 \\
TinyLlama 1.1B       & Local & $\sim$0.7\,GB~\checkmark & 5/5 \\
Gemini Flash         & API   & 0\,GB (hosted)           & 1/5 \\
\hline
\end{tabular}
\end{table}
 
The deciding hardware constraint was the group's weakest machine at 8\,GB RAM. At
approximately 2.5\,GB under 4-bit quantisation~\cite{google_gemma3_qat}, Gemma~3~4B runs
comfortably on every group member's laptop without requiring specialised hardware or GPU
resources. Mistral 7B Q4 was eliminated despite comparable quality because its
4.3\,GB footprint leaves insufficient headroom on 8\,GB systems once the OS and other
applications consume memory~\cite{mistral_vram}. Gemma~4 E4B was eliminated entirely as its
memory requirement sits at the 8\,GB ceiling~\cite{google_gemma4}. Phi-4 Mini fits the RAM
constraint but is optimised for reasoning and STEM tasks~\cite{phi4_mini_benchmark}, making
it a poor fit for the stylised generation mode. TinyLlama 1.1B, while the smallest option,
has been shown to struggle with nuanced synthesis tasks~\cite{tinyllama_eval}, which this
assignment requires.
 
The choice of a local model over a hosted API was deliberate. Gemma~3~4B has limited
knowledge of Shakespearean text, meaning that retrieval quality directly and visibly impacts
answer quality. When relevant passages are retrieved, the model produces accurate, grounded
answers. When retrieval fails, the model's response degrades noticeably. This makes the
comparison between the baseline and RAG system meaningful and the evaluation genuine. A
powerful hosted model such as Gemini Flash already has strong Shakespeare knowledge and may
produce correct answers even without retrieved context, which would make evaluation
inconclusive and undermine the purpose of the RAG pipeline.
 
Gemma~3~4B also demonstrates reliable instruction following and sufficient language quality
for both beginner-friendly explanation and stylised generation, making it appropriate for all
four interaction modes the system supports.

% ============================================================
\section{System Design and Implementation}
\label{sec:system}

\input{pipeline_diagram}

\subsection{System Pipeline}

All three systems share the same entry point and generation step, but
differ in how --- and whether --- they retrieve evidence before passing
anything to the language model. Figure~\ref{fig:pipeline} illustrates
the full pipeline.

\textbf{Baseline Pipeline.}
The user query is formatted into a prompt template and sent directly to
Gemma~3~4B via Ollama, with no retrieval step. As described in
Section~\ref{sec:methodology}, this pipeline serves as a controlled
comparison point rather than a performant system.

\textbf{Standard RAG Pipeline.}
The query is embedded into a 384-dimensional vector and used to search
the \texttt{shakespeare\_utterances} ChromaDB collection by cosine
similarity, returning the top-$k$ utterances with full metadata. Those
passages are formatted into a structured context block and injected into
a prompt template alongside the user question. The interaction mode
determines which template is used, with \texttt{qa} as the default.
Retrieved passages are always displayed before the generated answer,
regardless of mode.

\textbf{Reranked RAG Pipeline.}
The query embedding and ChromaDB search proceed identically to the
standard pipeline, but $3k$ candidates are retrieved in the first stage.
Those candidates are rescored by the cross-encoder described in
Section~\ref{sec:methodology}, re-sorted by relevance, and filtered by
the scene-level diversity constraint before the final top-$k$ passages
are passed to prompt construction and generation.

\subsection{Implementation Details}
The system is implemented in Python using three main scripts.
\texttt{baseline.py} handles the no-retrieval pipeline.
\texttt{rag\_chatbot.py} implements the standard RAG pipeline.
\texttt{rag\_chatbot\_reranked.py} implements the enhanced pipeline with
cross-encoder reranking. A shared entry point \texttt{main.py} presents
the user with three system options at startup --- baseline, standard RAG,
and reranked RAG --- defaulting to the standard RAG pipeline if no
selection is made. Within each RAG system, the user is prompted to select
an interaction mode by number (1~=~\texttt{qa}, 2~=~\texttt{concept},
3~=~\texttt{evidence}, 4~=~\texttt{stylised}) and specify $k$, the number
of passages to retrieve, before submitting a query. This numbered
selection eliminates the risk of input errors from free-text mode entry.
In the reranked pipeline, the system automatically retrieves $3k$ broad
candidates and reranks them down to the top $k$, preserving the two-stage
ratio regardless of the chosen $k$.
The core dependencies are \texttt{ollama} for local model inference,
\texttt{sentence-transformers} for query embedding and cross-encoder
reranking, \texttt{chromadb} for vector search, and \texttt{torch} as
the underlying runtime. Prompt templates are stored as plain text files
under \texttt{prompts/} and loaded at runtime, keeping prompt design
decoupled from pipeline logic.

The repository follows the structure recommended in the assignment
specification: raw and processed data under \texttt{data/}, pre-built
ChromaDB collections under \texttt{data/chroma\_utterances/}, source
scripts under \texttt{src/}, prompt templates under \texttt{prompts/},
and evaluation output under \texttt{results/}.

To run the system, three steps are required. The ChromaDB index is
already pre-built and committed to the repository, so no index
construction is needed at runtime:

\begin{enumerate}
    \item \texttt{pip install -r requirements.txt}
    \item \texttt{ollama pull gemma3:4b}
    \item \texttt{python src/main.py}
\end{enumerate}

The ChromaDB index is pre-built once and persisted to disk under
\texttt{data/chroma\_utterances/}. All three systems load the index at
startup with no recomputation required. Embedding the query at runtime
is the only computation that occurs per request --- the utterance
embeddings are already stored.

The system runs entirely on a standard laptop without GPU requirements.
Gemma~3~4B loads into approximately 2.5\,GB of RAM under Q4\_K\_M
quantisation, retaining approximately 97\% of full-precision quality
while reducing memory usage by 64\%. Generation typically takes
15--20 seconds per response on CPU, which is acceptable for a coursework
demonstration. The cross-encoder reranker adds noticeable latency on top
of generation time in the enhanced pipeline. A further limitation is that
the scene-level diversity constraint is implemented as a maximum cap
rather than a minimum coverage guarantee, which may reduce retrieval
quality on focused single-scene questions.


% ============================================================
%  GenAI Usage Log — Model and RAG Lead (Moinul)
%  CSCI433/933 Assignment 2 — Shakespeare-Aware RAG System
%  Appendix D — to be included in main report
% ============================================================

\section*{Appendix D: GenAI Usage Log --- Model and RAG Lead}

All prompts below are verbatim or lightly paraphrased where noted.
Summaries are written in my own words, not quoted from the AI output,
in line with the assignment's transparency requirements.

% ----------------------------------------------------------
\subsection*{Entry 1 --- Comparing Available Small Language Models}

\textbf{Prompt:}
\begin{quote}
``Give me a list of free to download small language models that I can use
to build a RAG pipeline. It must run locally on any standard computer.
Give me specifications like how many parameters, context window, etc.,
and a scale to judge their speciality.''
\end{quote}

\textbf{Summary of what it returned:}
Claude produced a comparison of seven locally runnable models: Gemma~3~4B,
Mistral~7B~Q4, Llama~3.2~3B, Phi-3.5~Mini, Qwen2.5~3B, TinyLlama~1.1B,
and Gemma~4~E4B. For each model it listed parameter count, context window
size, approximate RAM requirement at 4-bit quantisation, and a rough rating
across instruction following, literary quality, and how genuine the RAG
evaluation would be. The key insight it gave was that models with a
larger context window, like the 128k token window on Gemma~3~4B, can
comfortably hold multiple retrieved passages in a single generation call,
which is useful for this project. It also pointed out that all of these
models can be installed with a single \texttt{ollama pull} command,
which removes a lot of the setup friction.

\textbf{What I did with it:}
Used the comparison as a structured reference when evaluating candidates
against the group's hardware constraint. The table helped me see at a
glance which models were within reach and which were not. I did not use
the AI output directly in the report; instead I used it to narrow down
the shortlist and then did my own verification of the key figures.

\textbf{What I verified manually:}
Checked the RAM estimates against the Ollama model library page and
confirmed the download sizes listed there matched what Claude described.

% ----------------------------------------------------------
\subsection*{Entry 2 --- Why Gemma~3~4B Is the Right Choice for This Project}

\textbf{Prompt:}
\begin{quote}
``I think Gemma~3~4B is the most suitable choice for my project to answer
questions from Shakespeare literature retrieval. What are your opinions
on that?''
\end{quote}

\textbf{Summary of what it returned:}
Claude agreed and explained why, which was useful because it made the
reasoning concrete rather than just confirmatory. The core argument it
made was about evaluation genuineness. A model like Gemini Flash or
GPT-4o Mini has been trained on enormous amounts of text including
Shakespeare, so it can answer questions about Macbeth or Hamlet
reasonably well even without any retrieved context. That means the
baseline system would score nearly as high as the RAG system, and the
comparison would not prove much. Gemma~3~4B, on the other hand, has
limited Shakespeare knowledge, so when retrieval fails the answer gets
noticeably worse, and when retrieval works the answer improves. That
makes the experiment meaningful. Claude also noted the 128k context
window and the comfortable 2.5~GB RAM footprint as practical advantages.

\textbf{What I did with it:}
Used this reasoning directly as the basis for the model justification in
Section~III-C of the report. The argument about evaluation genuineness
is something I had thought about partially but Claude made it much
clearer. That is why that argument appears front and centre in the
justification rather than buried at the end.

\textbf{What I verified manually:}
Ran the model locally and asked ``Why does Macbeth kill Duncan?'' without
any retrieved context. The answer was vague and missed key details like
the role of the witches and Lady Macbeth. This confirmed that retrieval
would make a visible difference, which is exactly what the evaluation
needs to demonstrate.

% ----------------------------------------------------------
\subsection*{Entry 3 --- Assessing Model Size from Parameter Count}

\textbf{Prompt:}
\begin{quote}
``How to assess model size based on the number of parameters?''
\end{quote}

\textbf{Summary of what it returned:}
Claude explained the formula: model size in gigabytes equals the number
of parameters multiplied by the bits per parameter, divided by eight
billion. It introduced quantisation as a compression technique that
reduces bits per parameter from 32-bit full precision down to 4-bit,
which shrinks a 7B model from roughly 28~GB down to about 3.5~GB.
It gave a quick mental shortcut: parameters in billions multiplied by
0.5 gives the approximate GB needed at 4-bit. It also distinguished
between RAM (CPU memory) and VRAM (GPU memory), and noted that running
on CPU is slower but perfectly fine for coursework where generation
speed is not critical.

\textbf{What I did with it:}
Applied the formula to evaluate all candidate models against the 8~GB
constraint. This confirmed that Gemma~3~4B at roughly 2.5~GB is well
within range, while Gemma~4~E4B at roughly 8~GB sits right at the
ceiling with no headroom for the operating system and other processes.

\textbf{What I verified manually:}
Cross-checked the RAM estimates against the official Ollama model library
page and Google's QAT release notes, which confirmed the 2.5~GB figure
for Gemma~3~4B at Q4\_K\_M quantisation.

% ----------------------------------------------------------
\subsection*{Entry 4 --- Understanding Cross-Encoder vs Bi-Encoder}

\textbf{Prompt:}
\begin{quote}
``What does a cross encoder do differently than MiniLM-L6-v2?
They are both doing semantic search. What will it actually improve?''
\end{quote}

\textbf{Summary of what it returned:}
Claude explained that both models do semantic matching but in different
ways. The bi-encoder (MiniLM-L6-v2) encodes the query and each document
independently into separate vectors and compares them using cosine
similarity. The two texts never see each other during encoding. The
cross-encoder takes the query and document together as a single
concatenated input, which lets the transformer attend to interactions
between specific tokens across both texts. This produces more accurate
relevance scores but at a higher computation cost because you cannot
pre-compute the document vectors ahead of time. Claude illustrated this
with an example: a bi-encoder might rank the line ``Stars, hide your
fires'' low for the query ``Why does Macbeth kill Duncan?'' because the
exact words do not match, while a cross-encoder would recognise the
thematic connection and score it higher.

\textbf{What I did with it:}
Adopted the two-stage retrieve-then-rerank design based on this
explanation. The bi-encoder handles broad retrieval from ChromaDB
quickly, and the cross-encoder rescores only the shortlisted candidates
where accuracy matters most. This explanation directly shaped the
architecture of \texttt{rag\_chatbot\_reranked.py} and the justification
written in Section~III-B.

\textbf{What I verified manually:}
Ran both systems on the same query and observed that the reranked
pipeline surfaced different passages than the standard pipeline.

% ----------------------------------------------------------
\subsection*{Entry 5 --- How Many Results to Retrieve (Choosing k)}

\textbf{Prompt:}
\begin{quote}
``How do I decide how many top-k should I use for retrieval?
I know that the more k I choose, the slower output I will get.
But what is the sweet spot and how much is worth sacrificing?''
\end{quote}

\textbf{Summary of what it returned:}
Claude pointed out that the trade-off has three sides, not two. More k
means more relevant passages, but it also means more irrelevant ones
mixed in, and at some point the model cannot tell the useful passages
apart from the noise. It recommended k~=~5 for the standard RAG
pipeline and a retrieve-15-then-rerank-to-5 strategy for the reranked
pipeline. The reasoning was that the instructor questions are focused
on specific events and motivations, and the core evidence for any one
question typically fits within five passages. Going higher starts
pulling in tangentially related lines that add length without adding
clarity. For the reranked pipeline, retrieving more candidates first
gives the cross-encoder more to work with, so it can filter more
aggressively and return five high-confidence results rather than five
medium-confidence ones. Claude also mentioned that the proper way to
find the optimal k empirically is to sweep across k~=~3, 5, 8, and 10
and score each configuration, which could work as an extension if time
allowed.

\textbf{What I did with it:}
Kept k~=~5 for the standard pipeline and used 15-to-5 reranking for
the improved pipeline. These values are used in
\texttt{rag\_chatbot.py} and \texttt{rag\_chatbot\_reranked.py}
respectively. The three-sided trade-off framing (speed, relevance,
noise) was useful and I referenced it when writing the implementation
notes in Section~IV-B.

\textbf{What I verified manually:}
Ran the same query at k~=~3, 5, and 8 manually and observed that at
k~=~8 the retrieved evidence for Q1 included lines from unrelated
scenes that did not contribute to the answer. That confirmed k~=~5
was a reasonable stopping point for this corpus.

% ----------------------------------------------------------
\subsection*{Entry 6 --- How Embedding Models Handle Archaic Text}

\textbf{Prompt:}
\begin{quote}
``How will the embedding model handle archaic text?''
\end{quote}

\textbf{Summary of what it returned:}
Claude explained that during pretraining the embedding model learned
that words like ``kill'', ``murder'', ``slay'', and ``do the deed''
cluster together in vector space because they appear in similar
contexts across millions of training documents. That is why a modern
English query maps to roughly the same region of vector space as its
archaic equivalent. Without enrichment, a raw 10-word Shakespearean
utterance embeds into a relatively isolated region because the model
has too little context to place it accurately. The scene summaries and
keywords added during data preparation provide modern English context
that bridges this gap, ensuring utterances land near semantically
related modern queries.

\textbf{What I did with it:}
Used this explanation to add a clarifying passage in Section~III-B
explaining why enriched embedding strings were necessary and why the
system can retrieve relevant passages even when archaic words do not
appear in the query. It also strengthened the justification for the
data engineering team's embedding design, which I referenced when
writing the methodology section.

\textbf{What I verified manually:}
Tested the system with the query ``Why does Macbeth kill Duncan?'' and
confirmed that utterances containing words like ``deed'', ``dark
desires'', and ``ambition'' were retrieved even though ``kill'' and
``Duncan'' did not appear in them directly.

% ----------------------------------------------------------
\subsection*{Entry 7 --- What Fine-Tuning Would Actually Change}

\textbf{Prompt:}
\begin{quote}
``If I fine-tune the SLM, how will it improve the performance?
How will the parameters change and what will the extra layers represent?
Will it have more knowledge about Shakespeare or will it only predict
more Shakespearean tone as the next word?''
\end{quote}

\textbf{Summary of what it returned:}
Claude clarified that fine-tuning with LoRA or QLoRA does not add new
layers on top of the model. Instead it injects small trainable matrices,
called low-rank adapters, into the existing attention layers. The
original weights stay frozen and only these adapters are updated, adding
roughly one to ten million parameters on top of the four billion base
parameters. More importantly, Claude explained what those adapters
actually learn: they shift the probability distribution over the next
token toward outputs that look like the fine-tuning data. Fine-tuning
does not give the model new factual knowledge the way pretraining does.
It teaches the model to respond in a particular style and follow patterns
in the training examples. So fine-tuning on Shakespeare text would
improve the stylised generation mode because the model would learn to
produce Early Modern English constructions more naturally, but it would
not make the model more factually reliable about plot events. A
fine-tuned model without retrieval could still hallucinate, just now in
a more convincing Shakespearean register, which is arguably harder to
detect.

\textbf{What I did with it:}
This clarified why the current approach of RAG without fine-tuning is
the right design for the assignment. The goal is to demonstrate that
retrieval improves answers, not that the model has memorised the plays.
Fine-tuning would blur that experimental signal. I used this reasoning
to justify the decision to skip fine-tuning in Section~III-C, alongside
the practical constraint of the submission deadline.


% ----------------------------------------------------------
\subsection*{Entry 8 --- Why the Vector Space Has 384 Dimensions}

\textbf{Prompt:}
\begin{quote}
``Why does our vector space have 384 dimensions? What will happen
if we add more dimensions or take away some from this?''
\end{quote}

\textbf{Summary of what it returned:}
Claude explained that 384 comes from the architecture of
\texttt{all-MiniLM-L6-v2}, chosen by the researchers during pretraining
through experimentation. It is not a fundamental constant but a design
decision. Each dimension represents a learned axis of semantic variation,
and the meaning of a text is encoded across all 384 values together
rather than in any single one. If you increase dimensions, the model can
represent finer semantic distinctions, which is why larger models like
\texttt{text-embedding-3-large} use 3,072 dimensions. But for
Shakespeare retrieval the distinctions you need, such as murder versus
ambition versus betrayal, are broad enough that 384 handles them well.
If you reduce dimensions, distinct concepts start colliding in the
smaller space and retrieval quality degrades because previously
separated clusters begin overlapping. Claude also noted that the
dimension is baked into the model weights, so you cannot change it
without switching to a different embedding model entirely. The critical
constraint is consistency: the index and every query must go through the
same model, because vectors from different models are not comparable.

\textbf{What I did with it:}
Used this to understand why 384 is appropriate for this task and why
no change is needed. It also helped me write a cleaner explanation in
the methodology section of why \texttt{all-MiniLM-L6-v2} is sufficient
for the semantic range required by the three plays, without needing to
justify using a larger embedding model.

\textbf{What I verified manually:}
Confirmed the output dimension of the model by running a quick Python
check: \texttt{model.encode("test").shape} returns \texttt{(384,)},
matching what Claude described.

% ----------------------------------------------------------
\subsection*{Entry 9 --- What ``Surface Form'' Means}

\textbf{Prompt:}
\begin{quote}
``What does it mean with surface form and why it is relevant?''
\end{quote}

\textbf{Summary of what it returned:}
Claude explained that ``surface form'' is a linguistics term referring
to the literal written characters of a word as it appears on the page.
The words ``kill'', ``killeth'', and ``slayeth'' are different surface
forms but carry the same meaning. It suggested replacing the term with
simpler phrasing, such as ``regardless of how they are written or
phrased'', to keep the report accessible to readers without a
linguistics background.

\textbf{What I did with it:}
Replaced ``surface form'' in the Section~III-B draft with the simpler
phrasing. The report now reads ``regardless of how they are written or
phrased'', which fits the beginner-accessible tone the assignment
requires.

\textbf{What I verified manually:}
Re-read the paragraph in context to confirm the revised phrasing
preserved the intended meaning without losing technical accuracy.

\subsection*{Entry 10 --- Should the User Be Able to Choose the Retrieval Depth?}

\textbf{Prompt:}
\begin{quote}
``Should I let the user choose the top k?''
\end{quote}

\textbf{Summary of what it returned:}
Claude pointed out that the answer depends on who the user actually is.
The current \texttt{main.py} already exposes k as a configurable input,
so the mechanism is there. The question is whether that choice helps or
hurts. For the assignment demo and evaluation it is genuinely useful,
because you might want to test k~=~3 versus k~=~8 on the same question
to observe how retrieval depth affects answer quality, and that kind of
comparison is worth documenting in the report. But for a real end user
with no Shakespeare background, asking them to choose k is like asking
someone to choose their database page size before reading a document.
They have no basis for the decision, and if they pick a bad value the
answer degrades silently. Claude recommended keeping the input as it is
for demo and evaluation purposes, treating k~=~5 as the well-tested
default, and documenting the choice in Section~IV-B with reference to
manual testing across the instructor questions.

\textbf{What I did with it:}
Kept the configurable input in \texttt{main.py} as it is, since the
assignment context makes it useful for evaluation runs. Added a note
in Section~IV-B explaining that k~=~5 was selected based on manual
testing and that higher values introduced noise without improving answer
quality on the instructor question set.

\textbf{What I verified manually:}
Confirmed that the upper bound validation on k was already in place in
\texttt{main.py}, so a user entering an arbitrarily large value would
not cause the retrieval to crash or time out.
% ============================================================
\bibliographystyle{IEEEtran}
\bibliography{references}

\end{document}
